rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // อนุญาตทั้งหมดทุก collection — ไม่เจาะจงโฟลเดอร์ ไม่เช็ค role
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
